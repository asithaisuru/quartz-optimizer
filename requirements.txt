# --- Web Framework ---
fastapi==0.121.3
uvicorn==0.38.0
python-multipart==0.0.20
python-dotenv==1.2.1
requests==2.32.5

# --- AI & Computer Vision ---
ultralytics==8.2.103
rembg==2.0.68
onnxruntime-gpu==1.23.2
# Locked to 1.26.4 to prevent YOLO/PyTorch conflicts
numpy==1.26.4
# Headless version to prevent GUI errors on servers
opencv-python-headless==4.9.0.80
Pillow==11.3.0

# --- 3D Processing ---
open3d==0.19.0
trimesh==4.9.0
plyfile==1.1.3
scipy==1.15.3
# Triangulation engine trimesh needs to cap sliced sub-volume meshes
# (used by the leaf-piece repacking pass in optimizer.py)
mapbox_earcut==2.0.0
# Polygon operations used by trimesh.slice_mesh_plane(..., cap=True)
shapely==2.1.2

# Note: PyTorch and Rtree are installed via specific commands in README
# to ensure GPU support and C++ bindings are correct.
